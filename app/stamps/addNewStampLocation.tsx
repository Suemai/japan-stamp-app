import {Image, TextInput, View} from "react-native";
import React from "react";

const AddNewStampLocation = () => {

    return (
        <View style ={{flex: 1}}>

        </View>
    );
}

export default AddNewStampLocation;